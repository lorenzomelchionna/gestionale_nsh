# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# Pydantic
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
# 0.0.31, not 0.0.20: GHSA-5rvq-cxj2-64vf makes form parsing quadratic in the
# number of `;` separators, and the Twilio webhook has to parse the body before
# it can verify the signature — so the slow path is reachable pre-auth, on a
# single-worker deploy where it freezes every other request.
python-multipart==0.0.31

# Task queue
celery==5.4.0
redis==5.2.1
kombu==5.4.2

# Email
emails==0.6.0
jinja2==3.1.4

# HTTP client
httpx==0.28.1

# Utilities
python-dateutil==2.9.0
pytz==2024.2

# Image processing — every uploaded product photo is decoded and re-encoded by
# Pillow, so what gets stored is a file we produced rather than one we received.
pillow==11.0.0

# Monitoring
sentry-sdk[fastapi]==2.19.2
