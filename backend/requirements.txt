# Web framework
# 0.141.1 e non 0.115.6: la 0.115 trascina starlette 0.41, che aveva sette
# avvisi aperti — fra cui PYSEC-2026-249, il corpo urlencoded senza limiti su
# cui il webhook Twilio ha dovuto mettere un tetto a mano.
#
# Non è stato un aggiornamento di versione. La 0.141 non appiattisce più le
# rotte in `app.routes`: le avvolge in `_IncludedRouter`, e la matrice dei
# permessi in `tests/test_permissions_matrix.py` smetteva di vederle. È stata
# riscritta prima, e regge entrambe le versioni.
fastapi==0.141.1
uvicorn[standard]==0.52.1

# Database
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.5
psycopg2-binary==2.9.12

# Pydantic
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0

# Auth
# 3.5.0, non 3.3.0: PYSEC-2024-232 (confusione di algoritmo), PYSEC-2024-233
# e PYSEC-2025-185 (DoS su JWE). Nessuna delle tre era raggiungibile qui — i
# token sono JWS firmati HS256 e `jwt.decode` fissa l'algoritmo — ma la 3.5.0
# le chiude tutte e non costa niente, e restare su una versione con tre CVE
# note vuol dire tenerle in elenco per sempre a discutere ogni volta.
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
# 0.0.31, not 0.0.20: GHSA-5rvq-cxj2-64vf makes form parsing quadratic in the
# number of `;` separators, and the Twilio webhook has to parse the body before
# it can verify the signature — so the slow path is reachable pre-auth, on a
# single-worker deploy where it freezes every other request.
python-multipart==0.0.32

# Task queue
celery==5.6.3
redis==5.3.1
kombu==5.6.2

# Email
emails==0.6.0
# 3.1.6: tre evasioni dalla sandbox di Jinja (PYSEC-2026-1471/1472/1475).
# Qui i template li scriviamo noi e non arrivano dall'esterno, quindi nessuna
# era sfruttabile; l'aggiornamento è una patch.
jinja2==3.1.6

# HTTP client
httpx==0.28.1

# Utilities
python-dateutil==2.9.0
pytz==2024.2

# Image processing — every uploaded product photo is decoded and re-encoded by
# Pillow, so what gets stored is a file we produced rather than one we received.
# 12.3.0 e non 11.0.0: diciassette avvisi aperti sulla 11, fra cui scritture
# fuori dai limiti nei decoder PSD, GD, PCF/BDF e JPEG2000. La maggior parte
# non era raggiungibile da qui perché `services/images.py` passa
# `formats=("JPEG","PNG","WEBP")` a `Image.open`, e quei decoder non vengono
# nemmeno provati — ma non tutti: PYSEC-2026-3451 riguarda le API sulle
# coordinate e PYSEC-2026-3493 il codec `raw`, che stanno sul nostro percorso.
pillow==12.3.0

# Monitoring
sentry-sdk[fastapi]==2.66.1
slowapi==0.1.10
